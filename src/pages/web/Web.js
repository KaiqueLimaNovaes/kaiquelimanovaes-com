import styled from 'styled-components';

const Title = styled.h1`
  font-size: 20px;
  font-weight: bold;
  color: #1d6bff;
`;

const Web = () => {
    return(
        <Title>Web</Title>
    );
}

export default Web;