import styled from 'styled-components';

const Title = styled.h1`
  font-size: 20px;
  font-weight: bold;
  color: #1d6bff;
`;

const Mobile = () => {
    return(
        <Title>Mobile</Title>
    );
}

export default Mobile;